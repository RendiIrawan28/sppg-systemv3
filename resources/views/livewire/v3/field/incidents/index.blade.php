<x-v3.shell :$unit :$navigation :$roleLabel title="Insiden Lapangan" eyebrow="Temuan lintas divisi dan tindak lanjut">
    <div class="mx-auto mb-5 max-w-[1450px]"><x-v3.date-filter label="Tanggal insiden" /></div>
    <div class="mx-auto max-w-[1450px] space-y-5"><x-v3.flash-alert /><div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="text-xs font-bold uppercase tracking-[.18em] text-sky-700">Lapangan</p><h2 class="mt-2 text-2xl font-bold text-slate-950">Insiden operasional</h2><p class="mt-1 text-sm text-slate-500">Catat kejadian, penanggung jawab, tindakan langsung, dan penyelesaiannya.</p></div>@if($canCreate)<a wire:navigate href="{{ route('v3.field.incidents.create') }}" class="h-11 rounded-xl bg-sky-600 px-5 py-3 text-sm font-bold text-white">Catat insiden</a>@endif</div>
        <section class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-100 text-sm"><thead class="bg-slate-50"><tr><th class="px-5 py-3 text-left text-[10px] font-bold uppercase text-slate-500">Kejadian</th><th class="px-5 py-3 text-left text-[10px] font-bold uppercase text-slate-500">Divisi</th><th class="px-5 py-3 text-left text-[10px] font-bold uppercase text-slate-500">Tingkat</th><th class="px-5 py-3 text-left text-[10px] font-bold uppercase text-slate-500">Status</th><th></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($incidents as $incident)<tr><td class="px-5 py-4"><p class="font-bold text-slate-900">{{ $incident->title }}</p><p class="mt-1 text-xs text-slate-400">{{ $incident->occurred_at?->translatedFormat('d M Y H:i') }} · {{ $incident->location ?: 'Tanpa lokasi' }}</p></td><td class="px-5 py-4 text-slate-600">{{ str($incident->division_code)->headline() }}</td><td class="px-5 py-4"><span class="rounded-full bg-amber-50 px-2.5 py-1 text-[10px] font-bold text-amber-700">{{ $incident->severity->label() }}</span></td><td class="px-5 py-4"><span class="rounded-full bg-slate-100 px-2.5 py-1 text-[10px] font-bold text-slate-600">{{ $incident->status->label() }}</span></td><td class="px-5 py-4 text-right"><a wire:navigate href="{{ route('v3.field.incidents.show', $incident) }}" class="text-xs font-bold text-sky-700">Buka</a></td></tr>@empty<tr><td colspan="5" class="px-5 py-14 text-center text-slate-400">Belum ada insiden.</td></tr>@endforelse</tbody></table></div>@if($incidents->hasPages())<div class="border-t border-slate-100 p-4">{{ $incidents->links() }}</div>@endif</section>
    </div>
</x-v3.shell>
